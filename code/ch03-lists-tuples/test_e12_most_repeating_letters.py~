from e12_most_repeating_letters import most_repeating_letter_count
import pytest


@pytest.mark.parametrize('args, output', [
    ((), ()),
    ((10, 20, 30), 60),
    (('a', 'b', 'c'), 'abc'),
    ([10, 20, 30], 60)
])
def test_mysum(args, output):
    assert mysum(*args) == output
