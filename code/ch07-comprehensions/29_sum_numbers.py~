#!/usr/bin/env python3

numbers = input("Enter integers, separated by spaces: ")

total = sum(int(number) < 1 >
            for number in numbers.split() < 2 >
            if number.isdigit()) < 3 >

print(f"Sum: {total}")
